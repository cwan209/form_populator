# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`order_populater` is a tool for populating orders from spreadsheet data. The primary input is `input.xlsx`.

## Current State

The project is in early setup. No source code exists yet — only `input.xlsx` (the input data asset).
